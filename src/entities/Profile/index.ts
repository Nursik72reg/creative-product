import { ProfileSchema, Profile } from './model/types/profile';
import { profileReducer, profileAction } from './model/slice/profileSlice';

export {
    ProfileSchema, Profile, profileReducer, profileAction,
};
